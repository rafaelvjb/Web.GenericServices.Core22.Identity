@model IEnumerable<WebMultiContext.DTOs.PessoaDto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone.Numero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone.Numero)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.PessoaId }) |
                @Html.ActionLink("Details", "Details", new { id = item.PessoaId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.PessoaId })
            </td>
        </tr>
}
    </tbody>
</table>
